<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cameraControls="using:CameraControls">

    <Style TargetType="cameraControls:LabeledImage">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="cameraControls:LabeledImage">
                    <Border Background="LightBlue" BorderBrush="Black" BorderThickness="2" 
                            HorizontalAlignment="Center" Width="300" Height="200">
                        <StackPanel HorizontalAlignment="Center">
                            <Image Stretch="Uniform" Width="100" Height="100" 
                                   Source="{TemplateBinding ImagePath}" Margin="5" />
                            <TextBlock TextAlignment="Center" FontFamily="Segoe UI" FontWeight="Light" 
                                       FontSize="26.667" Foreground="Black" x:Uid="/SimpleCustomControl/Resources/LabelHeader" />
                            <TextBlock TextAlignment="Center" 
                                       Text="{TemplateBinding Label}" FontFamily="Seqoe UI" FontWeight="Light" 
                                       FontSize="26.667" Foreground="Black" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
